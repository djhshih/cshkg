#install packages
install.packages("readr")

#library installation
library(readr)

#load expression data
x <- read.tsv("tcga-pancan-expr.tsv")

# map probe to genes

# remove NA genes


# remove duplicated genes